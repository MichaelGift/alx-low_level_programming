#include "main.h"
/**
 * add - add two integers and return the result
 * @a: first int param
 * @b: second int param
 * Return: a+b
 */
int add(int a, int b)
{
	int result = 0;

	result = a + b;
	return (result);
}
